# OPTIONAL sponsor-stack SDKs. The core demo does NOT need any of these — every
# adapter lazy-imports its SDK and no-ops if absent. Install only what you enable:
#   pip install -r requirements-sponsor.txt
sentry-sdk>=2.7           # ENABLE_SENTRY
redis>=5.0                # ENABLE_REDIS_EVENTS
anthropic>=0.39           # ENABLE_CLAUDE_ASSIST
arize>=7.0                # ENABLE_ARIZE_LOGGING
conductor-python>=1.1     # ENABLE_ORKES_SHADOW
deepgram-sdk>=3.0         # ENABLE_DEEPGRAM_VOICE
# RunPod uses its plain HTTP API via `requests` (already a core dep) — no SDK needed.
